---
/**
 * ✅ WarningIcon Component
 *
 * @description Warning icon inside a rounded square.
 *
 * @usage
 * ```astro
 * <WarningIcon />
 * <WarningIcon className="size-6 text-yellow-500" />
 * ```
 *
 * @compatible
 * - 🔘 Can be used in Button component
 * - ✅ Useful for forms, checklists, and success states
 * - 🎨 Supports all SVG className props
 */
import { Svg } from "@/components/atoms";

interface Props {
  className?: string;
}

const { className } = Astro.props;
---

<Svg className={className}>
  <path
    fill="none"
    stroke="currentColor"
    stroke-linecap="round"
    stroke-linejoin="round"
    stroke-width="2"
    d="m21.73 18l-8-14a2 2 0 0 0-3.48 0l-8 14A2 2 0 0 0 4 21h16a2 2 0 0 0 1.73-3M12 9v4m0 4h.01"
  ></path>
</Svg>
