---
/**
 * 🌗 Theme Element
 * 
 * @description Theme toggle with auto-switching capability.
 * All logic is handled by the Svelte component for better reactivity.
 * 
 * @usage
 * ```astro
 * <Theme />
 * <Theme showAutoSwitch={true} />
 * ```
 * 
 * @props
 * - showAutoSwitch: boolean - Show the auto-switch toggle (default: false)
 * 
 * @compatible
 * - 🎨 Toggles .dark/.light class on <html>
 * - 💾 Persists preference in localStorage
 * - 🌙 Uses system theme by default if no preference saved
 * - ✨ Smooth transitions between themes
 */
import { ThemeIcon } from "@/components/icons";

---

<button
  id="toggle-theme-button"
  aria-label="Toggle theme"
  class="cursor-pointer"
>
  <ThemeIcon className="size-5" />
</button>
